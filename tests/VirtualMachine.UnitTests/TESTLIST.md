# Список тестов модуля VirtualMachine (DEA)

## Остановка вычислений
- [x] Остановка виртуальной машины с нулевым кодом возврата
- [x] Остановка виртуальной машины с ненулевым кодом возврата

## Негативные (Остановка вычислений)
- [x] Ошибка при выполнении инструкции возврата (return) вне контекста функции

## Стековые вычисления выражений
- [x] Возврат одного значения со стека
- [x] Сложение и вычитание с помощью стека
- [x] Умножение, деление и остаток от деления (modulo)
- [x] Целочисленное деление с помощью стека
- [x] Возведение числа в степень
- [x] Унарный минус в арифметическом выражении
- [x] Операции над вещественными числами (тип `num`)
- [x] Конкатенация строк с помощью стека
- [ ] Удаление значения с вершины стека (`pop`)
- [x] Ошибка при выполнении арифметики над несовместимыми типами
- [x] Ошибка при делении на ноль

## Встроенные функции
- [x] Функция `print`
- [x] Функция `input`
- [x] Функция `len`
- [x] Функция `substr`
- [x] Функция `abs`
- [x] Функции `min` и `max`

## Негативные (Встроенные функции)
- [x] Ошибка при вводе значения, не соответствующего целевому типу (bool/int/num)

## Переменные и константы
- [x] Сохранение и загрузка значений переменных (`mutable`)
- [x] Сохранение и загрузка значений констант (`immutable`)
- [x] Ошибка при попытке записи значения в константу
- [x] Ошибка при чтении из неинициализированной переменной
- [x] Ошибка при повторном объявлении идентификатора в одной области видимости
- [x] Ошибка при несоответствии типов при присваивании

## Негативные (Переменные и константы)
- [x] Ошибка при попытке чтения пользовательского ввода в константу 
- [x] Защита от объявления переменной с некорректным системным тегом типа